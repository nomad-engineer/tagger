PyQt5>=5.15.0
Pillow>=9.0.0
platformdirs>=3.0.0
pyparsing>=3.0.0
pytest>=7.0.0
imagehash
phash
scikit-learn
scipy
timm
pyspellchecker
opencv-python

# Optional: Required for Model Tagging plugin (AI-powered captioning)
# These packages are large and will be auto-installed when the plugin is first used
# Uncomment to install in advance:
 transformers>=4.30.0
 torch>=2.0.0
 torchvision>=0.15.0
 accelerate>=0.20.0
 sentencepiece
 protobuf
